# =============================================================================
# 环境变量配置模板
# 用途：复制为 .env 后填写真实值
#
# 部署位置：/opt/project/{{PROJECT_NAME}}/.env
# 安全：chmod 600 /opt/project/{{PROJECT_NAME}}/.env
#
# 模板变量说明：
#   {{PROJECT_NAME}}   项目名
#   {{DOMAIN}}         完整访问域名
# =============================================================================

# ---------------------------------------------------------------------------
# 应用域名（Traefik 路由用）
# ---------------------------------------------------------------------------
DOMAIN={{PROJECT_NAME}}.example.com

# ---------------------------------------------------------------------------
# 数据库（公用 PostgreSQL）
# ---------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD={{DB_PASSWORD}}
POSTGRES_DB={{PROJECT_NAME}}

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD={{REDIS_PASSWORD:-}}

# ---------------------------------------------------------------------------
# 服务间通信（内部网络）
# ---------------------------------------------------------------------------
# 后端服务通过 internal 网络访问其他服务
# 例：AI 服务 → BACKEND_API_URL=http://ai-job-hunter-be:8080
BACKEND_API_URL=http://ai-job-hunter-be:8080

# ---------------------------------------------------------------------------
# 应用配置
# ---------------------------------------------------------------------------
APP_PORT={{APP_PORT}}
LOG_LEVEL=info

# ---------------------------------------------------------------------------
# 安全密钥（必填，生产环境使用强随机值）
# ---------------------------------------------------------------------------
# JWT 密钥（生成：openssl rand -base64 32）
JWT_SECRET={{JWT_SECRET}}

# 加密密钥（生成：openssl rand -hex 32）
ENCRYPTION_KEY={{ENCRYPTION_KEY}}

# ---------------------------------------------------------------------------
# 外部 API Keys（按需）
# ---------------------------------------------------------------------------
# OPENAI_API_KEY={{LLM_API_KEY}}
# ANTHROPIC_API_KEY={{ANTHROPIC_API_KEY}}

# ---------------------------------------------------------------------------
# GitHub Container Registry（CI/CD 动态注入，无需手动配置）
# ---------------------------------------------------------------------------
# IMAGE_NAMESPACE={{GITHUB_ORG}}

# ---------------------------------------------------------------------------
# 镜像版本（由 docker-tag.env 管理，不要直接修改）
# ---------------------------------------------------------------------------
# IMAGE_TAG=env-20260910
