# =============================================================================
# Docker Tag 升级历史模板
# 用途：给运维/开发记录镜像版本升级轨迹，复制内容到 .env 使用
#
# 维护规则：
#   1) 最新版本放第一行（唯一生效）
#   2) 历史版本只注释，不删除
#   3) 新版本发布时，插入到第一行；旧第一行改成注释
#   4) 标签建议格式：env-YYYYMMDD 或 env-YYYYMMDD-vN
#
# 服务器权限配置（deploy 用户仅对此文件有写权限）：
#   setfacl -m u:deploy:rw /opt/project/{{PROJECT_NAME}}/docker-tag.env
#
# 部署位置：/opt/project/{{PROJECT_NAME}}/docker-tag.env
# =============================================================================

IMAGE_NAMESPACE={{GITHUB_ORG}}
IMAGE_TAG=env-20260910

# 历史版本（注释掉的为已废弃）
# IMAGE_TAG=env-20260901
# IMAGE_TAG=latest
