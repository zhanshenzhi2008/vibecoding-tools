---
description: Agent 开发使用反馈问题汇总，记录实际开发中遇到的问题和解决方案
alwaysApply: false
globs:
  - data/system/agt/**/*.txt
  - **/resources/system/agt/**/*.txt
---

# Agent 开发使用反馈规则

本文档记录 Agent 开发过程中遇到的真实问题及解决方案。

---

## 规则1：when() 条件使用 FreeMarker 语法，不支持 Groovy 安全导航 `?.`

**when() 条件的表达式引擎是 FreeMarker，不是 Groovy**。

关键限制：
- **不支持** `?.` 安全导航操作符（这是 Groovy 语法）
- FreeMarker 的 null 安全语法是 `(xxx)!''` 或 `xxx??`

```groovy
// ❌ 错误：FreeMarker 不支持 ?. 安全导航
when('n_agt_result?.is_plan_break == "true"', { ... })

// ✅ 正确：用 act_func?script 处理，Groovy 上下文中可直接访问 Map 属性
n_should_break, act_func?script='''
  def isBreak = n_agt_result?.is_plan_break == "true"
  fn.put("n_should_break", isBreak ? "true" : "false")
  return isBreak
'''
when('fn.bool(n_should_break)', {
  act_flow.go?plan=break
})
```

---

## 规则2：when() 条件中不能用方法调用

**报错示例**：`Unexpected input: '():'`

`.toLowerCase()`、`.contains()` 等方法**不能**直接写在 `when('...')` 条件里。

```groovy
// ❌ 错误
when('contextSystem.toLowerCase().contains("windows")', { ... })

// ✅ 正确：先用 act_func 计算布尔值
n_os, act_func?script='''
  return contextSystem?.toLowerCase()?.contains("windows") ? 'windows' : 'linux'
'''
when('n_os == "windows"', { ... })
```

---

## 规则3：Elvis 操作符 `?:` 和三元运算符 `? :`

**报错示例**：
```
Unexpected input: '():' @ line 2, column 41.
   def len = n_message_data?.size():0
```

```groovy
// ❌ 错误：? 紧跟 () 后面写 :0，Groovy 误解
def len = n_message_data?.size():0

// ✅ 正确：Elvis 操作符（推荐）
def len = n_message_data?.size() ?: 0

// ✅ 正确：三元运算符（? 和 : 之间必须有空格）
def len = n_message_data ? n_message_data.size() : 0
```

---

## 规则3.1：`{{ }}` 模板中不要写 `?:`、`? :`、嵌套 `{{ }}` 或 `fn.xxx(..., fn.xxx(...))`

**适用范围**：`assign('x', '{{...}}')`、prompt 模板、Agent 调用参数、`fn.json(...)` 外层模板等所有 `{{ }}` 插值位置。

**原因**：`{{ }}` 是模板插值语法，不是 Groovy 表达式块。
在 `{{ }}` 里写 Elvis 操作符 `?:`、三元表达式 `? :`、再嵌套一个 `{{ }}`，或写 `fn.xxx(..., fn.xxx(...))` 这类函数参数里再套 `fn.xxx(...)`，都容易导致模板解析失败、可读性差，或生成错误内容。

```groovy
// ❌ 错误：在 {{ }} 中写 Elvis 操作符
assign('n_name', '{{userName ?: "anonymous"}}')

// ❌ 错误：在 {{ }} 中写三元表达式
assign('n_env', '{{contextEnv ? contextEnv : "dev"}}')

// ❌ 错误：在一个 {{ }} 中嵌套另一个 {{ }}
assign('n_result', '{{fn.json("answer, {{n_answer}}")}}')

// ❌ 错误：fn 参数里再嵌套 fn
assign('n_result', '{{fn.optional(a, fn.optional(b, c))}}')

// ✅ 正确：默认值逻辑拆成前置变量或分步 assign
assign('n_name', '{{fn.optional(userName, "anonymous")}}')
assign('n_env', '{{fn.optional(contextEnv, "dev")}}')
assign('n_answer_safe', '{{n_answer}}')
assign('n_result', '{{fn.json("answer, n_answer_safe")}}')
assign('n_a_safe', '{{fn.optional(a, "")}}')
assign('n_b_safe', '{{fn.optional(b, "")}}')
```

**建议**：
- `{{ }}` 内只放单层变量引用或单层 `fn.xxx(...)` 调用
- 不要在 `fn.xxx(...)` 的参数里再写 `fn.xxx(...)`
- 默认值逻辑优先拆成前置变量
- 复杂判断先用 `assign` / `act_func?script` 算出结果，再在 `{{ }}` 中引用

---

## 规则4：terminal_command 参数引号规则

**报错原因**：`||`、`|` 在裸值中会被 DSL 解析器识别为运算符。

```groovy
// ❌ 错误：含 | 的命令会报错
terminal_command=where bash | findstr /i "Git"

// ✅ 正确：含 | 必须先 assign 再引用
assign('n_cmd', 'where bash | findstr /i "Git"')
terminal_command={{n_cmd}}

// ✅ 简单命令可直接裸值
terminal_command=git --version

// ✅ 含空格/特殊字符必须加引号
terminal_command='where bash 2>nul'
```

---

## 规则5：多行内容用三引号英文符号

使用 **英文单三引号** `'''` `'''` 或 **英文双三引号** `"""` `"""`（必须成对出现）。

```groovy
// ✅ 正确：三单引号多行
n_steps, act_plan.parse?task='''
- [ ] find_project
- [ ] check_git
'''

// ✅ 正确：三双引号多行
n_steps, act_plan.parse?task="""
- [ ] find_project
- [ ] check_git
"""

// ❌ 错误：单引号多行（会被解析为普通字符串）
n_steps, act_plan.parse?task='
- [ ] find_project
'
```

---

## 规则6：每行有效代码必须换行

后端使用 Java 解析 agent 文件，**每一行有效代码必须换行**。

```groovy
// ✅ 正确：有效代码单独一行
when('fn.bool(n_should_break)', {
  act_flow.go?plan=break
})

// ❌ 错误：有效代码写在一行
when('fn.bool(n_should_break)', { act_flow.go?plan=break })

// ✅ 正确：多个有效代码分别换行
when('n_step.item == "find_project"', {
  n_agt_result, agt_find_project.assistant?...
  n_should_break, act_func?script='''
    def result = n_agt_result ?: ""
    def isBreak = result.contains('"is_plan_break":"true"')
    fn.put("n_should_break", isBreak ? "true" : "false")
    return isBreak
  '''
  when('fn.bool(n_should_break)', {
    act_flow.go?plan=break
  })
})

// ❌ 错误：多个有效代码写在一行
when('n_step.item == "find_project"', { n_agt_result, agt_find_project... })
```

**总结**：
- `when()` 的条件后，第一个 `{` 和条件在同一行
- `{ }` 内的有效代码必须换行
- 不要在一行内写多个有效代码（用分号分隔也不行）
- 注释和空行可以单独一行

---

## 规则7：script 块内反斜杠需双倍转义

**报错示例**：`Unexpected character: '''`

三引号块内容会经过 DSL 转义预处理（`\\` → `\`），**块内要表示一个 `\` 必须写 `\\\\`**。

```groovy
// ❌ 错误：'\\' 经过DSL处理变成 '\'，Groovy报 Unexpected character
path.replace('\\', '/')

// ✅ 正确：'\\\\' 经过DSL处理变成 '\\'，Groovy看到一个反斜杠
path.replace('\\\\', '/')
```

---

## 规则8：Windows 路径统一转为正斜杠

**报错原因**：`{{n_path}}` 插入模板时反斜杠会被当成转义序列吃掉，`\g`→`g`。

```groovy
// ❌ 错误
F:\project\name → 插入后变成 F:projectname

// ✅ 正确：在 Groovy 脚本里转换
def forwardSlashPath = path.replace('\\\\', '/')
fn.put('n_project_root', forwardSlashPath)
```

---

## 规则9：assign() 字符串里不要用 `\\` 拼路径

**报错原因**：`\\` 会被转义消耗。

```groovy
// ❌ 错误
assign('n_cmd', 'deploy\\build.bat')  // 变成 deploybuild.bat

// ✅ 正确：使用正斜杠
assign('n_cmd', 'deploy/build.bat')
```

---

## 规则10：含空格路径需要 `\"` 包裹

```groovy
// ✅ 正确
assign('n_deploy_cmd', 'cd \"{{n_project_root}}/deploy\" && bash startup.sh')
```

---

## 规则11：fn.list() 解析 CLI runner 返回值

`agt_local_cli_command_runner` 返回 JSON 数组字符串（如 `["F:/path\n"]`）。

```groovy
// ✅ 正确
def list = fn.list(n_result)
return list ? list[0]?.trim() : null

// ❌ 错误：不要用这个
fn.jsonObj(n_result)
```

---

## 规则12：Groovy 脚本内写变量用 fn.put()

在 `act_func?script='''...'''` 里，用 `fn.put('varName', value)` 写入上下文变量。

```groovy
n_find_ok, act_func?script='''
  def path = fn.list(n_find_result)?[0]?.trim()
  if (!path) return false
  fn.put('n_project_root', path.replace('\\\\', '//'))
  return true
'''
```

---

## 规则13：foreach 内用 act_flow.go?plan=break 提前退出

```groovy
when('n_find_ok', {
  // 成功逻辑
}, {
  assign('n_deploy_result', '❌ 失败原因')
  act_flow.go?plan=break   // 中断 foreach
})
```

---

## 规则14：减少 when() 嵌套，用提前退出代替

```groovy
// ❌ 嵌套深
when('condition', {
  // 大量主逻辑...
}, {
  assign('n_result', '不支持')
})

// ✅ 提前退出，主逻辑在顶层
when('!condition', {
  assign('n_result', '不支持')
  act_flow.go?plan=end
})
// 主逻辑直接写，少一层缩进
```

---

## 规则15：多层引号嵌套规则

**报错示例**：`Unexpected input` 或解析错误

多层嵌套时，**不能嵌套相同的引号**：
- 单引号 `'...'` 里面不能再放单引号 `'`
- 双引号 `"..."` 里面不能再放双引号 `"

```groovy
// ❌ 错误：双引号里再嵌套双引号
act_memory.assign?var=n_result&value="{{n_cmd == ""? n_cmd: n_cmd2}}"&type=code

// ✅ 正确：内双外单
act_memory.assign?var=cmd_executor_result&value="{{n_result.command == ''? n_result.command: n_result.commands}}\n{{n_result.stderr}}"&type=code

// ✅ 正确：内单外双
act_memory.assign?var=n_result&value='{{n_cmd == ""? n_cmd: n_cmd2}}'&type=code
```

**`{{}}` 内联表达式的引号规则同样适用：**
- `{{}}` 内的引号与外面的引号**不能相同**
- 外部用单引号 `'`，内部字符串用双引号 `""`
- 外部用双引号 `"`，内部字符串用单引号 `''`

```groovy
// ✅ 正确：外单内双
assign('n_has_error', '{{n_output?.contains("error")}}')

// ✅ 正确：外双内单
assign('n_has_error', "{{n_output?.contains('error')}}")

// ❌ 错误：外单内单
assign('n_has_error', '{{n_output?.contains('error')}}')  // 会解析错误
```

**最佳实践**：优先使用**内双外单**，可以解决更复杂的格式。

---

## 规则16：`{{}}` 内联表达式中不能用 `+` 拼接字符串

`{{}}` 模板内是 Freemarker 语法，**不能用 `+` 拼接字符串**。应该直接在模板中写变量或使用其他方式。

```groovy
// ❌ 错误：{{}} 内用了 + 拼接
assign("n_executor_return", "⚠️ 错误：{{n_reason}}\n\n命令：" + (cmd ?: "") + "\n\n请人工操作。")

// ✅ 正确：在 Groovy 脚本里拼接，然后返回
n_result, act_func?script='''
def msg = "⚠️ 危险命令已拦截：" + (n_danger_reason ?: "") + "\n\n命令：" + (cmd ?: "") + "\n\n如果必要，请人工操作。"
fn.put("n_executor_return", msg)
return msg
'''
```

**原则**：复杂字符串拼接应该在 `act_func?script` 中完成，不要在 `{{}}` 模板内使用 `+`。

---

## 规则17：`{{}}` 是 Freemarker 模板语法，`act_func?script` 才是 Groovy 语法

| 语法 | 实际引擎 | 说明 |
|---|---|---|
| `{{}}` | Freemarker | 模板语法，简单变量和表达式 |
| `act_func?script='''...'''` | Groovy | 复杂逻辑，用 `fn.put()` 写入变量 |

**常见误解**：`{{}}` 里面的代码不是 Groovy 语法，而是 Freemarker 表达式语法。

**复杂表达式处理原则**：
- 简单变量输出用 `{{变量名}}`
- 复杂表达式优先使用 `{{fn.xxx()}}` 自定义工具方法
- **禁止**在 `fn.xxx()` 后面继续调用 Java 方法，如 `{{fn.list(n_data).size()}}` ❌
- 复杂逻辑用 `act_func?script`

```groovy
// {{}} - Freemarker 模板语法
assign('n_upper', '{{n_name}}')  // 直接输出变量值
assign('n_str', '{{fn.str(n_result)}}')  // 使用 fn 工具方法
// ❌ 错误：fn.xxx() 后面不能调用 .size()
assign('n_len', '{{fn.list(n_data).size()}}')

// ✅ 正确：复杂逻辑用 act_func?script
n_result, act_func?script='''
def list = fn.list(n_data)
return list?.size() ?: 0
'''
```

**原则**：
- 简单变量输出用 `{{}}`
- 复杂表达式优先用 `{{fn.xx()}}`
- 复杂逻辑用 `act_func?script`

```groovy
n_result, act_func?script='''
  def output = fn.str(n_input) ?: ""
  def valid = output.trim().equals("VALID")
  fn.put('n_is_valid', valid)
  return valid
'''
```

---

## 规则18：`fn.optional` 只支持2参数，不支持3参数

**报错示例**：`Method optional(java.lang.String,java.lang.String,null) cannot be found`

```groovy
// ❌ 错误：fn.optional 不支持3参数
assign('n_effective_server_id', '{{fn.optional(server_id, n_g_server_id, n_server_id)}}')

// ✅ 正确：用 act_func?script 实现多参数优先级
n_effective_server_id, act_func?script='''
    def sid = (server_id ?: "").trim()
    def gsid = (n_g_server_id ?: "").trim()
    def lsid = ""
    try {
        lsid = ((n_server_id ?: "") as String).trim()
    } catch(e) {
        lsid = ""
    }

    def result = sid
    if (sid.isEmpty()) {
        result = !gsid.isEmpty() ? gsid : lsid
    }

    fn.put("n_effective_server_id", result)
    return result
'''
```

---

## 规则19：Freemarker 访问可能为 null 的属性会报错

**报错示例**：`The following has evaluated to null or missing: ==> n_result_data.commands`

```groovy
// ❌ 错误：Freemarker 中访问不存在的属性会报错
act_memory.assign?var=n_executor_return&value="{{((n_result_data.command == '')?then(n_result_data.commands, n_result_data.command))}}\n{{n_result_data.stderr}}"&type=code

// ✅ 正确：用 act_func?script 安全访问
n_fail_msg, act_func?script='''
    def cmd = (n_result_data?.command ?: "").trim()
    def cmds = (n_result_data?.commands ?: "").trim()
    def err = (n_result_data?.stderr ?: "").trim()

    def finalCmd = cmd
    if (cmd.isEmpty()) {
        finalCmd = cmds
    }

    def msg = finalCmd + "\n" + err
    fn.put("n_executor_return", msg)
    return msg
'''
```

**原则**：如果对象属性可能为 null，应该在 Groovy 脚本中用 `?.` 安全访问，不要在 Freemarker `{{}}` 中直接访问。

---

## 规则20：`act_func?script` 块内代码缩进规范

`act_func?script='''...'''` 块内的代码应该缩进4个空格，保持 Java/Groovy 代码风格。

```groovy
// ✅ 正确：脚本内缩进4空格
n_is_dangerous, act_func?script='''
    def c = (cmd ?: "").trim().toLowerCase()
    def d = false
    def reason = ""

    if (c.contains("rm -rf /")) {
        d = true
        reason = "递归删除根目录"
    }

    fn.put("n_danger_reason", reason)
    return d
'''

// ✅ 简单逻辑可压缩成一行
n_danger_msg, act_func?script='''def msg = "..."; fn.put("n_executor_return", msg); return msg'''
```

---

## 规则21：全局变量命名规则 `n_g_驼峰`

当 `act_memory.assign` 使用 `global=true` 存入全局变量时，命名规则为 `n_g_` + 驼峰变量名。

```groovy
// ✅ 正确：全局变量用 n_g_ 前缀，驼峰命名
act_memory.assign?var=n_g_serverId&value={{server_id}}&type=code&global=true
act_memory.assign?var=n_g_userName&value={{user_name}}&type=code&global=true

// 读取时用同样的命名
n_g_serverId, act_memory.retrieve?var=n_g_serverId&global=true
n_g_userName, act_memory.retrieve?var=n_g_userName&global=true
```

**命名规范**：
- 全局变量：`n_g_` + 驼峰，如 `n_g_serverId`、`n_g_userName`
- 普通变量：`n_` + 驼峰，如 `n_server_id`、`n_user_name`（下划线也可）

---

## 规则22：外部 Agent 调用返回值为 Map 对象，需用 act_func 解析

当使用 `n_result, agt_xxx.yy?...` 调用外部 agent 时，`n_result` 的值是**被调用 agent 最后一句有效代码的返回值（Map 对象）**。

**重要**：`n_agt_result` 是 Map 对象，在 `act_func?script` 的 Groovy 上下文中可直接访问属性，但在 FreeMarker `{{}}` 和 `when()` 条件中**不能直接访问**。

```groovy
// agt_xxx.txt 最后一句
assign("n_return_result", '{"n_log":"{{n_log}}","n_data":"值","is_plan_break":"false"}')

// 调用方接收返回值（Map 对象）
n_agt_result, agt_xxx.yy?param=value

// ❌ 错误：FreeMarker 不支持直接 Map 属性访问
assign('n_log', '{{n_agt_result.n_log}}')

// ❌ 错误：FreeMarker 不支持 ?. 安全导航
when('fn.hasValue(n_agt_result.n_data)', { ... })

// ✅ 正确：用 act_func?script 提取属性到独立变量
n_result_log, act_func?script='''
  def log = n_agt_result?.n_log ?: ""
  fn.put("n_result_log", log)
  return log
'''
assign('n_log', '{{n_result_log}}')

// ✅ 正确：用 act_func?script 处理判断
n_should_break, act_func?script='''
  def isBreak = n_agt_result?.is_plan_break == "true"
  fn.put("n_should_break", isBreak ? "true" : "false")
  return isBreak
'''
when('fn.bool(n_should_break)', {
  act_flow.go?plan=break
})
```

**推荐返回值格式**：
```groovy
// 成功
assign("n_return_result", '{"n_log":"{{n_log}}","n_data":"值","is_plan_break":"false"}')

// 失败
assign("n_return_result", '{"n_log":"{{n_log}}","n_data":"","is_plan_break":"true"}')
```

---

## 规则23：act_plan.run 调用结果判断

`act_plan.run` 底层调用返回的是 `TaskResult.java` 对象，序列化后格式如下：

```json
{"status":"SUCCESS","result":null,"source":"agt_xxx.assistant"}
```

### 关键说明

| 字段 | 说明 |
|-----|------|
| `status` | 执行状态：`SUCCESS` / `FAIL` |
| `result` | **业务结果**，即被调用 agent 的 `resultPlan` 变量值 |
| `source` | 被调用的 agent 名称 |

**注意**：`act_plan.run` 返回的对象**没有 `success` 字段**，只有 `status` 字段。

### 错误示例

```groovy
// ❌ 错误：act_plan.run 没有 success 字段
when('n_tool_exec.success == true', { ... })
```

### 正确示例

```groovy
// ✅ 正确：使用 status 字段判断
when("n_tool_exec.status == 'SUCCESS'", {
    // 成功逻辑
}, {
    // 失败逻辑
})
```

### 如果需要判断子 agent 执行成功或失败

被调用 agent 的 `resultPlan` 是**业务结果**，不包含执行状态。需要在被调用 agent 的脚本中主动包装返回值，或在调用方使用 `on_failure` 捕获异常：

```groovy
// ✅ 方式1：被调用方主动包装返回值
// agt_xxx.txt 最后一句
assign("n_return_result", '{"status":"SUCCESS","result":"{{n_deploy_result}}","is_plan_break":"false"}')

// ✅ 方式2：调用方使用 on_failure 捕获异常
// 捕捉所有前序异常,将当前流程转为正常状态。（可使用act_flow.fail再次将流程转为异常状态）
on_failure({
    assign('resultTip', '{"status":"FAIL","result":{{n_plan_run_tool_exec.result}},"source":"{{confirmed_tool.name}}"}')
    // goto语句，跳转到代码结尾
    // 支持内置标记：this(重新执行当前步骤)，next(下一步)，break，continue，end(跳转到agent的结束位置)，exit(终止整个流程)
    act_flow.go?plan=end
})
```

**总结**：如果被调用 agent 需要返回执行成功/失败状态给调用方，必须在脚本中主动包装包含 `status` 字段的 JSON 返回值，而不是依赖框架隐式的执行状态。

---

## 规则25：`_plainProperty` 参数保护模板不被二次解析

### 核心用途

`_plainProperty` 用于指定某些参数**不做模板解析**，只做 trim，保护参数值中的 `{{}}` 不被二次解析。

### 什么时候需要用

| 场景 | 是否需要 | 原因 |
|-----|:---:|------|
| 参数值不含 `{{}}` | ❌ | 无需保护 |
| 参数值含 `{{}}` 且需要解析 | ❌ | 需要被解析 |
| 参数值含 `{{}}` 但需保持原样 | ✅ | 保护模板不被二次解析 |
| 嵌套 JSON 中含 `{{}}` | ✅ | 外层 JSON 会被 str() 序列化，内层模板需保护 |

### 典型场景：ReAct Agent 中的 params 透传

当 `n_react_turn` 变量包含 JSON，其中 `params` 字段可能含有 `{{}}` 时：

```groovy
// n_react_turn 示例值：
// {"action":"agt_xxx","params":{"table":"{{table_name}}","sql":"SELECT * FROM {{table}}"}}

// ❌ 错误：不加 _plainProperty，系统会尝试解析 params 里面的 {{table_name}}
// 如果 memory 中没有这个变量，会导致解析失败或得到意外值
n_dispatch_result, act_plan.run?code={{react_action}}&parameters={{fn.str(n_react_turn)}}

// ✅ 正确：指定 _plainProperty=parameters，让 parameters 保持原样不做模板解析
assign('n_react_turn._plainProperty', 'params')
n_dispatch_result, act_plan.run?code={{react_action}}&parameters={{fn.str(n_react_turn)}}
```

### 多参数时只保护特定参数

```groovy
// 只保护 parameters，其他参数仍做模板解析
n_tool_exec, act_plan.run?toolType={{toolType}}&code={{toolCode}}&parameters={{n_params}}&_plainProperty=parameters
```

### 注意事项

- `_plainProperty` 的值是逗号分隔的字段名列表，如 `_plainProperty=param1,param2`
- 被保护的参数只做 trim，不会调用 `ConversionUtil.format` 进行模板解析
- 如果参数值本身就是变量引用（如 `{{parameters}}`），需要确保 `parameters` 变量本身已经解析完毕

---

## 规则26：when() 条件语法分类与限制

`WhenPlanExecutor.java` 第 63-82 行的解析逻辑：

| 条件格式 | 解析方式 | 示例 |
|---------|---------|------|
| 纯变量（`[a-zA-Z0-9_.!]+`） | 直接取值 | `n_flag`, `n_step.item` |
| `fn.xxx(...)` 开头 | 直接作为 FreeMarker 函数 | `fn.bool(xxx)`, `fn.hasValue(xxx)` |
| `<#...>` 开头 | 完全由 FreeMarker 控制 | `<#if xxx>...</#if>` |
| 其他表达式 | 用 `fn.str()` 包裹 | `n_step.item == "find_project"` |

**FreeMarker 语法限制**：
- ❌ **不支持** `?.` 安全导航操作符（Groovy 语法）
- ❌ **不支持** `.contains()`、`.toLowerCase()` 等方法调用
- ❌ **不支持** 直接 JSON 对象属性访问 `n_agt_result.n_log`
- ✅ **支持** 三元运算符 `condition ? value1 : value2`
- ✅ **支持** `(xxx)!''` 默认值语法

---

## 规则27：`assign` 赋值 JSON 字符串优先使用 `fn.json()`

### 问题场景

`assign()` 用单引号直接拼接 JSON 字符串，只有当变量值**不含特殊字符**（引号、换行等）时才能正常工作：

```groovy
// ✅ 仅适合变量值无特殊字符的场景
assign('resultTip', '{"status":"SUCCESS","result":"{{n_plan_run_tool_exec}}","source":"{{confirmed_tool.name}}"}')
```

当变量值包含引号、换行等特殊符号时，上述写法容易导致 JSON 语法错误。

### 推荐写法：使用 `fn.json("k1,v1,k2,v2")`

`fn.json()` 接受单个字符串参数 `keyRefValue`，格式为逗号分隔的 `key,refValue,key,refValue...`，按顺序组装 JSON 对象，**自动处理转义**：

```groovy
// ✅ 推荐：用 fn.json 组装，自动处理特殊字符
// fn.json 参数内含单引号和双引号时，外层 assign 用三引号包裹，避免引号冲突
assign('resultTip', '''{{fn.json("status,'SUCCESS',result,n_plan_run_tool_exec,source,confirmed_tool.name")}}''')
```

**引号处理原则**：
- `fn.json` 参数内使用**双引号包裹 key**、**单引号包裹静态 value**（或反过来）
- `assign` 外层使用**三引号 `'''...'''`** 包裹整个值，避免与内层引号冲突

```groovy
// ❌ 错误：外层单引号与 fn.json 内单引号冲突
assign('resultTip', '{{fn.json(\'status,SUCCESS,result,n_var\')}}')

// ❌ 错误：外层双引号与 fn.json 内双引号冲突
assign('resultTip', "{{fn.json(\"status,SUCCESS,result,n_var\")}}")

// ✅ 正确：外层用三引号包裹
assign('resultTip', '''{{fn.json("status,'SUCCESS',result,n_plan_run_tool_exec,source,confirmed_tool.name")}}''')
```

### 语法说明

`fn.json` 只有一个参数，写法固定为：

```
fn.json('key1,refValue1,key2,refValue2,...')
```

**refValue 有三种写法**，系统按首尾字符自动区分：

| refValue 写法 | 含义 | 示例 |
|---|---|---|
| 裸变量名 | 从 memory 中按变量名取值 | `n_plan_run_tool_exec` |
| 双引号包裹 `"xxx"` | 静态字符串字面量 | `"操作成功"` |
| 单引号包裹 `'xxx'` | 静态字符串字面量 | `'操作成功'` |
| 斜杠包裹 `/xxx/` | 静态字面量，逗号不参与分割，避免多重引号转义 | `/Coding/agent/` |

```groovy
// ✅ 变量值：直接写变量名，不加 {{}}；外层 assign 用三引号包裹
assign('resultTip', '''{{fn.json("status,'SUCCESS',result,n_plan_run_tool_exec,source,confirmed_tool.name")}}''')

// ✅ 静态值：用双引号包裹静态 value
assign('n_result', '''{{fn.json("status,'SUCCESS',msg,操作成功")}}''')

// ✅ 混合使用：变量和静态字面量混排
assign('n_result', '''{{fn.json("status,'SUCCESS',result,n_tool_exec_result,msg,操作成功")}}''')

// ✅ 静态值含特殊字符（如路径含逗号）：用斜杠包裹
assign('n_result', '''{{fn.json("folderPath,/Coding/agent/build/")}}''')
```

**注意**：`fn.json` 只有一个 String 参数，**不支持多参数写法**。

```groovy
// ❌ 错误：fn.json 不支持多参数
fn.json('status', 'SUCCESS', 'result', n_data)  // 没有这种写法！

// ✅ 正确：全部写在一个逗号分隔的字符串里
fn.json('status,SUCCESS,result,n_data')
```

### 完整示例对比

```groovy
// ❌ 不推荐：变量值含特殊字符时易出错
assign('resultTip', '{"status":"SUCCESS","result":"{{n_plan_run_tool_exec}}","source":"{{confirmed_tool.name}}"}')

// ✅ 推荐：fn.json 自动处理转义，外层用三引号包裹
assign('resultTip', '''{{fn.json("status,'SUCCESS',result,n_plan_run_tool_exec,source,confirmed_tool.name")}}''')

// ✅ 静态值用双引号包裹，外层三引号
assign('n_return', '''{{fn.json("code,0,msg,操作成功,data,n_data")}}''')

// 当 value 本身需要是 JSON 字符串时，fn.json 会自动序列化
assign('n_return', '''{{fn.json("status,'SUCCESS',result,n_data,source,agt_xxx.assistant")}}''')
```

### 选择原则

| 场景 | 写法 |
|------|------|
| 变量值简单、无特殊字符 | `assign('x', '{"key":"{{var}}"}')` 简单 JSON 字符串 |
| 变量值可能含引号/换行等特殊字符 | `assign('x', '''{{fn.json("key,var,key2,static")}}''')` 外层三引号 |
| 静态值含特殊字符（逗号等） | 用斜杠包裹：`fn.json('key,/static,with,comma/')` |
| 结构非常复杂 | 用 `act_func?script` + Groovy 代码构造 |

---

## 快速检查清单

| 检查项 | 状态 |
|-------|------|
| when() 条件没用方法调用 | ☐ |
| when() 条件没用 `?.` 安全导航 | ☐ |
| when() 条件没用 JSON 属性访问 | ☐ |
| 三元运算用了 `?:` 或 `? :`（有空格） | ☐ |
| 含 `\|` 的命令用了 assign 变量 | ☐ |
| 多行内容用了三引号 `'''` 或 `"""` | ☐ |
| script 块内反斜杠用了 `\\\\` | ☐ |
| Windows 路径转为了正斜杠 `/` | ☐ |
| 含空格的路径用了 `\"` | ☐ |
| CLI 返回值用了 `fn.list()` | ☐ |
| 多层引号嵌套用了外双内单 | ☐ |
| `{{}}` 内联表达式引号与外部不同 | ☐ |
| `fn.optional` 只用了2参数 | ☐ |
| 可能为null的属性访问用了 Groovy script | ☐ |
| Windows 下用 Git Bash | ☐ |
| `_plainProperty` 只用于需要保护的参数 | ☐ |
| JSON 赋值优先使用 `fn.json()` | ☐ |

---

## 相关规则

- **注释标签规则**：`agent_comment_tags_rules.mdc`
- **编码规范**：`agent_file_encoding_rules.mdc`
- **Shell 执行规则**：`windows-shell-rules.mdc`
