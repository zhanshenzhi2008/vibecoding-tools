---
alwaysApply: false
---
## 修改规范

### 1. 禁止事项

| 禁止操作 | 原因 |
|---|---|
| ❌ 改变文件编码 | 导致 IDEA 编译失败 |
| ❌ 添加 BOM 头 | Java 编译器报错 |
| ❌ 转换行尾符（可选） | 可能影响 Git diff |
| ❌ 自动格式化整个 Java 文件 | 可能引入大量无关 diff |
| ❌ 在代码中直接写全限定类名 | 降低可读性，增加维护成本 |

### 2. 正确做法

```java
// ✅ 保持原有编码，直接修改代码
// 只修改需要改动的部分，不要格式化整个文件

// ✅ 添加新代码时使用原有风格
// 保持缩进、命名风格一致

// ✅ 修改时保留原有注释
// 不要随意删除或修改原有的业务注释
```

### 3. 编辑时同步检查

- 优先做最小化修改，只改与当前问题直接相关的代码
- 修改前尽量观察同文件或同模块已有写法，保持风格一致
- 新增 import 时，优先使用 `import`，避免在代码中写全限定类名
- 只有在 `import` 冲突且无法清晰解决时，才允许使用全限定类名
- 涉及注释新增或修改时，遵循项目现有 Java 注释规范
- 修改后优先检查本次改动是否引入编译错误、明显告警或不必要 diff

### 4. 格式化规则

- **不要**自动格式化整个文件（可能改变行尾符）
- 只格式化改动的部分
- 如需格式化，事先告知用户并获得确认

### 4.1 Java import 使用规则

- 新增 Java 代码时，优先通过 `import` 引入类，再直接使用类名
- **不要**在代码中直接写全限定类名（如 `java.util.regex.Matcher`），除非存在 `import` 冲突且无法通过调整 import 清晰解决
- 示例：优先使用 `import java.util.regex.Matcher;`，代码中写 `Matcher.quoteReplacement(...)`

### 4.2 与下游规则保持一致

- 本规则聚焦 **Java 代码编辑行为**，用于约束修改方式、diff 控制、import 与编码处理
- 涉及 Java 业务开发、注释、Entity、QLInfo、事务、工具类优先级等规范时，同步参考 `java-development-rules.mdc`
- 如两份规则存在重叠内容，优先保持表述一致，避免冲突与重复定义

---

## 常见问题

### 问题：BOM 导致编译失败

```
错误: 非法字符: '\ufeff'
public class Test {
```

**原因**：文件保存为 `UTF-8 with BOM`

**解决**：用 VSCode 重新 Save As 为 `UTF-8`

### 问题：中文乱码

**原因**：文件编码与 IDEA 编码不一致

**解决**：
1. 确认文件是 `UTF-8` 编码
2. IDEA 设置：`Settings → Editor → File Encodings → UTF-8`

---

## 快速检查清单

- [ ] 修改文件时右下角显示 `UTF-8`？
- [ ] 没有添加 BOM 头？
- [ ] 没有改变原有编码格式？
- [ ] IDEA 能正常编译此文件？
