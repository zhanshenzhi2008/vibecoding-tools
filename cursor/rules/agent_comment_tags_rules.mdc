---
description: Agent 脚本注释标签展示与折叠规则说明
alwaysApply: false
globs:
  - data/system/agt/**/*.txt
  - **/resources/system/agt/**/*.txt
---

# Agent 注释标签规则

## 概述

Agent 脚本中以 `//` 开头的行有不同用途。`//#`、`//##`、`//###`、`//^`、`//$` 开头的行会在前端 Chat 页面展示，**支持 Freemarker 模板语法 `{{}}`**。

**折叠规则**：
- `//` - 普通注释，不展示
- `//#` - 展示注释，**不折叠**
- `//##` / `//###` - 展示注释，**折叠下一行正常代码的执行结果**
- `//^` / `//$` - 展示注释，不折叠

## 标签分类

| 标签 | 含义 | 展示 | 折叠 |
|---|---|---|---|
| `//` | 普通注释 | **不展示** | - |
| `//#` | 一级步骤标题 | 展示 | **不折叠** |
| `//##` | 二级步骤标题 | 展示 | 折叠下一行**正常代码** |
| `//###` | 三级步骤标题 | 展示 | 折叠下一行**正常代码** |
| `//^` | 首个回应 | 展示 | 不折叠 |
| `//$` | 结果回应 | 展示 | 不折叠 |

---

## Freemarker 模板语法

展示标签支持 `{{变量名}}` 格式的 Freemarker 模板，变量会被实际值替换。

```groovy
//# [{{n_executor_mode}}] {{cmd_executor_name_text}}  // 不折叠
//## [{{n_executor_mode}}] {{cmd_executor_name_text}} - 成功  // 折叠
```

---

## 详细说明

### 1. `//` - 不展示

普通注释，不会在前端展示。

```groovy
// 这是内部注释，前端不展示
assign('n_var', 'value')  // 行尾注释也不展示
```

---

### 2. `//#` - 一级标题（不折叠）

一级步骤标题，前端直接展示注释内容，**不折叠**。

```groovy
//# 项目部署流程
```

**前端展示**：`📌 项目部署流程`

---

### 3. `//##` - 二级标题（折叠）

二级步骤标题，前端展示标题（支持模板变量），**下一行必须是正常代码**才会折叠展示其执行结果。

**语法**：`//## 标题文本`（后面必须紧跟正常代码行）

```groovy
//## [{{n_executor_mode}}] {{cmd_executor_name_text}} - 成功
act_memory.assign?var=n_executor_return&value={{n_result_data.stdout}}&type=code
```

**前端展示**：
```
📌 [云端] 执行命令 - 成功          （有折叠图标）
   └ 执行成功                       （展开后显示）
```

---

### 4. `//###` - 三级标题（折叠）

三级步骤标题，同 `//##`，下一行正常代码结果折叠展示。

---

### 5. `//^` - 首个回应（不折叠）

表示该 plan 的输出应显示在「首个回应区域」。

```groovy
//^ 您好！我是智能部署助手
return "请告诉我您要部署的项目名称"
```

---

### 6. `//$` - 结果回应（不折叠）

表示该 plan 的输出应显示在「结果显示区域」。

```groovy
//$ 部署完成
return "项目已成功部署到 172.18.2.14"
```

---

## 折叠下一行代码的要求

### 必须是「正常代码」

折叠的下一行**必须是正常代码**，即：
- ✅ 以字母、数字开头（如 `assign`、`n_result`、`when`）
- ✅ 以 `n_`、`n_g_` 等变量前缀开头
- ❌ **不能是** `//` 开头（普通注释）
- ❌ **不能是** `/*` 开头（块注释）
- ❌ **不能是** 空行

### 折叠判定表

| 下一行内容 | 是否折叠 |
|---|---|
| `assign('n_var', 'value')` | ✅ 折叠 |
| `n_result, act_func?script=...` | ✅ 折叠 |
| `when('...', {...})` | ✅ 折叠 |
| `// 普通注释` | ❌ 不折叠 |
| `/* 块注释 */` | ❌ 不折叠 |
| 空行 | ❌ 不折叠 |

### 正确写法

```groovy
// ✅ 正确：//## 后紧邻正常代码行
//## 检测环境
n_result, act_func?script='return "windows"'

// ✅ 正确：//### 后紧邻正常代码行
//### 检查配置
n_check, act_func?script='return "ok"'
```

### 错误写法

```groovy
// ❌ 错误：标签行后是空行（不会折叠）
//## 检测环境

n_result, act_func?script='return "windows"'

// ❌ 错误：标签行后是普通注释（不会折叠）
//## 检测环境
// 这是注释
n_result, act_func?script='return "windows"'

// ❌ 错误：标签行后是块注释（不会折叠）
//## 检测环境
/* 多行注释
   内容
*/
n_result, act_func?script='return "windows"'
```

---

## 完整示例

### 命令执行器示例

```groovy
//# [{{n_executor_mode}}] {{cmd_executor_name_text}} - 执行中
act_memory.assign?var=cmd_text&value={{cmd}}&type=code

//## [{{n_executor_mode}}] {{cmd_executor_name_text}} - 结果
act_memory.assign?var=n_executor_return&value={{n_result_data.stdout}}&type=code
```

**前端展示**：
```
📌 [云端] 执行命令 - 执行中          （不折叠）

📌 [云端] 执行命令 - 结果             （有折叠图标）
   └ 执行成功                          （展开后显示）
```

### 部署流程示例

```groovy
//# 智能打包部署

//## 检测操作系统
n_os, act_func?script='return "windows"'

//## 检查 Git 环境
n_git_check, agt_cmd_executor?cmd=git --version&cmd_executor_name="检查Git版本"

//## 检查 Maven 环境
n_mvn_check, agt_cmd_executor?cmd=mvn --version&cmd_executor_name="检查Maven版本"

//$ 打包部署完成
return "BUILD SUCCESS，耗时 5分30秒"
```

**前端展示**：
```
📌 智能打包部署                     （不折叠）

📌 检测操作系统                      （有折叠图标）
   └ windows

📌 检查 Git 环境                     （有折叠图标）
   └ git version 2.40.0

📌 检查 Maven 环境                    （有折叠图标）
   └ Maven 3.9.6

✅ 打包部署完成                      （不折叠）
   └ BUILD SUCCESS，耗时 5分30秒
```

---

## 快速检查清单

- [ ] 普通注释是否使用 `//`（不展示）？
- [ ] 一级标题是否使用 `//#`（展示，不折叠）？
- [ ] 需要折叠的步骤是否使用 `//##` 或 `//###`？
- [ ] 标签行后是否紧邻**正常代码**（不是 `//`、`/*` 开头或空行）？
- [ ] Freemarker 变量是否使用 `{{}}` 包裹？
