---
description: LLM-Agent 系统 Agent 脚本创建规范
alwaysApply: true
---

# LLM-Agent 脚本创建规范

---

## 命名规则

* 扩展名：`txt`

---

## 存储路径

* **默认根路径**：`F:\gientech-repository\llm-agent\agent-platform\src\main\resources\system\agt`
* **子目录**：`...\agt\chatdoc\XXX`，其中 XXX 为自定义包名，按功能/模块命名
* **外部根目录**（无特殊指定时）：`D:\app\project\llm-agent\data\system\agt`

---

## 参数声明规则

* `parameter` 中只声明**外部调用时需要传入**的参数
* `assign` 在 agent 内部定义的变量**无需**在 `parameter` 中重复声明

---

## 语法参考

### act_xxx / agt_xxx 语法

`act_xxx.yyy`、`agt_xxx.yyy` 中的 xxx 是 `com.llm.agentcore.core.action.ITool` 定义的常量类。

**命名规则来源**：`llm-agent-core\src\main\java\com\llm\agentcore\core\action` 工程下 Tool.java 后缀命名的类的 public 方法。

**示例**：
```
act_kb.search?requirement=&name=&column&outputFields=
```
表示调用后端代码 `com.llm.agentcore.core.action.KbTool` 的 search 方法，参数为 requirement、name、column、outputFields。

### fn.xxx() 语法

`{{fn.xxx(a,b,c)}}` 是 FunctionUtil 类方法。

**注意**：虽然支持 Groovy 语法，但**不支持** `new groovy.json.JsonBuilder` 这种引用包的 JSON 转化。尽量用 `fn.list`、`fn.map`、`fn.str` 等 FunctionUtil 里的方法进行转化。

### 语法参考文件

1. `F:\gientech-repository\llm-agent-core\src\main\resources\system\agt\meta\agent_grammar.txt`
2. `D:\app\project\llm-agent\data\system\agt\template\agt_common_template.md常用语法说明文件.md`

> **注意**：编码规范请参考 `agent_file_encoding_rules.mdc`，Shell 执行规则请参考 `windows-shell-rules.mdc`。

---

## Agent 调用语法

### 外部 Agent 调用规则

**调用格式**：`agt_XXX?参数1=值1&参数2=值2`

或使用**带包名**的调用格式（推荐，避免名称冲突）：

**格式**：`agt_XX.子包1.子包2?参数=值`

**说明**：
- `/system/agt/chatdoc/demo/agt_hello.txt` → 调用 `agt_hello.chatdoc.demo?参数`
- `/system/agt/smart_build_deploy/agt_check_git.txt` → 调用 `agt_check_git.smart_build_deploy?参数`

### 示例

```groovy
// ✅ 直接调用（同一目录下）
agt_hello?user={{name}}

// ✅ 带包名调用（推荐，可避免名称冲突）
agt_check_git.smart_build_deploy?os={{n_os}}

// ✅ 多级包名
agt_hello.chatdoc.demo?msg=hello

// ❌ 错误写法：使用 / 分隔
smart_build_deploy/agt_check_git?os={{n_os}}  // 这是错误的！
```

### 包名对应规则

| 文件路径 | 包名 | 调用语法 |
|---------|------|---------|
| `.../agt/assistant/agt_xx.txt` | `assistant` | `agt_xx.assistant?参数` |
| `.../agt/chatdoc/demo/agt_xx.txt` | `chatdoc.demo` | `agt_xx.chatdoc.demo?参数` |
| `.../agt/a/b/c/agt_xx.txt` | `a.b.c` | `agt_xx.a.b.c?参数` |

---
