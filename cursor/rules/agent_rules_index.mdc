---
description: Cursor / Claude / Codex 跨工具 Agent Rules 索引
description: zh: Cursor / Claude / Codex 等多种 Agent 开发工具的全局规则索引
globs:
alwaysApply: true
---

# Agent Rules 索引

> **用法**：本规则 `alwaysApply: true`，Agent 在启动时会自动加载。它本身不写具体规则，
> 只作为**所有 Agent 共享配置的目录页**——具体规则通过 `@<file>` 反向引用。
>
> **统一源**：`cursor/rules/` 下的规则文件，通过 symlink/复制对接到 Claude / Codex / 其他工具。

## 📁 目录结构

```
vibecoding-tools/
├── cursor/                       ← 唯一真实源
│   └── rules/                    ← 全部规则在这里
└── ~/.cursor/skills/ → cursor/skills/   ← 全局 symlink
```

> ⚠️ 修改规则只改 `cursor/rules/<name>.mdc` 一份。Claude / Codex 通过同名 symlink 同步。

---

## 📋 规则文件清单

| 规则 | 说明 | 文件 |
|------|------|------|
| Agent 注释标签规则 | AI 生成的代码注释应该如何打标签 | `@agent_comment_tags_rules.mdc` |
| Agent 反馈规则 | 反馈 / 错误信息的格式规范 | `@agent_feedback_rules.mdc` |
| Agent 文件编码 | 文件读写的字符编码规范 | `@agent_file_encoding_rules.mdc` |
| Agent 脚本生成 | 创建/修改 Agent 脚本的统一规范 | `@create-agent-script.mdc` |
| LLM Agent 日志 | 日志格式、级别、保留策略 | `@llm-agent-logs.mdc` |
| 数据库升级 | 数据库 schema 迁移规则 | `@database_upgrade_rules.mdc` |
| Java 开发 | Java 开发的最佳实践集合 | `@java-development-rules.mdc` |
| Java 代码编辑 | Java 代码改动的格式和流程 | `@java_code_edit_rules.mdc` |
| Java 注释命名 | Java 注释的统一命名规范 | `@java_comment_naming_rules.mdc` |
| Windows Shell | Windows PowerShell / CMD 专用规则 | `@windows-shell-rules.mdc` |

---

## 🤖 跨工具规则加载机制

### Cursor
- 直接扫描 `cursor/rules/` 与 `.cursor/rules/` → 自动加载

### Claude Code
- 项目内规则：`./CLAUDE.md` 或 `./.claude/rules/`
- 如果使用 symlink：`ln -sf vibecoding-tools/cursor/rules ~/.claude/rules`
- 用户全局规则：`~/.claude/CLAUDE.md`
- 引用规则文件用 `@cursor/rules/<name>.mdc`

### Codex（OpenAI）
- 项目内规则：`./AGENTS.md`
- 如果使用 symlink：`ln -sf vibecoding-tools/cursor/rules ~/.codex/rules`
- 引用：`@cursor/rules/<name>.mdc`

---

## ✏️ 如何新增 / 修改规则

1. **新增规则**：在 `cursor/rules/` 下新建 `<name>.mdc`，确保 frontmatter 完整
2. **修改规则**：直接编辑 `cursor/rules/<name>.mdc`
3. **同步本索引**：在上面的"规则文件清单"表格里加一行
4. **提交**：开 PR，按 `CODEOWNERS` 指定的 owner review
5. **其他工具同步**：symlink 自动同步，无需手动复制

---

## 🔒 规则文件安全审查

任何 `cursor/rules/` 的修改必须符合 `.github/PULL_REQUEST_TEMPLATE.md` 的审查清单，
且 `.github/workflows/cursor-security-scan.yml` 扫描通过。

修改 `agent_rules_index.mdc`（本文件）**需要双重 review**——因为它影响 Agent 的全局行为。

---

## 📝 模板复制提示

> **不要直接复制本规则当 CLAUDE.md 或 AGENTS.md 使用**。本文件是 Cursor 规则格式，
> 各工具需要自己的入口文件。请：
>
> - Cursor：`cursor/rules/` 下其他规则通过 `@<file>` 反向引用本索引
> - Claude Code：在项目根 `./CLAUDE.md` 中 include 本规则
> - Codex：在项目根 `./AGENTS.md` 中 include 本规则
