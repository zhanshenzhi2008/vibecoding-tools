---
description: Agent 脚本文件创建规范
alwaysApply: true
globs:
  - **/system/agt/**/*.txt
---

# Agent 脚本文件创建规范

## 编码要求：必须使用 UTF-8 without BOM

**重要**：所有 `.txt` agent 脚本文件必须保存为 **UTF-8 无 BOM 编码**。

### 为什么？

带有 BOM（`EF BB BF`）的文件会导致 Groovy 解析器报错：
```
Unexpected character: '﻿' @ line 14, column 9
```

### 如何确保？

1. **VSCode 设置**：在用户设置 `settings.json` 中添加：

```json
{
  "files.encoding": "utf8",
  "files.insertFinalNewline": true
}
```

2. **创建文件后检查**：用以下命令检查是否有 BOM：

```bash
# 检查文件开头是否为 EF BB BF
head -c 3 filename.txt | xxd
```

3. **有 BOM 时移除**：用 VSCode 重新 Save As，选择 "UTF-8"（不带 signature）

### 其他注意事项

- agent 脚本扩展名必须是 `.txt`
- 存储路径：`F:\gientech-repository\llm-agent\agent-platform\src\main\resources\system\agt\chatdoc\XXX`
- 语法参考：`F:\gientech-repository\llm-agent-core\src\main\resources\system\agt\meta\agent_grammar.txt`
