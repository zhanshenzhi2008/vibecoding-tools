---
description: Windows 环境下优先使用 Git Bash 执行 Shell 命令的全局规则
alwaysApply: true
---

# Windows Shell 执行规则

## 核心原则

**Windows 环境优先使用 Git Bash，不使用 PowerShell。**

PowerShell 存在语法差异、路径转义和编码乱码问题，容易导致命令执行失败或异常结果。

---

## Git Bash 信息

| 项目 | 值 |
|------|-----|
| **路径** | `D:\Program Files\Git\bin\bash.exe` |
| **用途** | 执行所有 Shell 命令（ls, grep, git, cat 等） |

---

## 执行方式

### 方式1：bash -c（推荐）

```groovy
assign('n_cmd', 'ls -la /f/gientech-repository')
n_result, agt_local_cli_command_runner.local_agent_client_cli?terminal_command='bash -c "{{n_cmd}}"'
```

### 方式2：直接调用 bash.exe

```groovy
assign('n_cmd', 'git status')
n_result, agt_local_cli_command_runner.local_agent_client_cli?terminal_command='"D:\\Program Files\\Git\\bin\\bash.exe" -c "{{n_cmd}}"'
```

---

## 路径规范

| 规范 | 说明 |
|------|------|
| **使用正斜杠 `/`** | Git Bash 兼容 Windows 路径 |
| **避免 `\\`** | `\\` 会被转义消耗 |
| **空格路径用 `\"`** | 含空格的路径需要转义包裹 |

### 正确示例

```groovy
// ✅ 正确：正斜杠
assign('n_path', 'F:/gientech-repository/logs')

// ✅ 正确：含空格路径
assign('n_cmd', 'cd "F:/Program Files/Git" && ls')
```

### 错误示例

```groovy
// ❌ 错误：反斜杠会被转义
assign('n_path', 'F:\\gientech-repository\\logs')

// ❌ 错误：deploy\\build.bat 会变成 deploybuild.bat
assign('n_cmd', 'deploy\\build.bat')
```

---

## 含管道符的命令

含 `|` 的命令必须先用 assign 变量存储，再引用：

```groovy
// ✅ 正确
assign('n_cmd', 'grep "error" app.log | head -20')
n_result, agt_local_cli_command_runner.local_agent_client_cli?terminal_command='bash -c "{{n_cmd}}"'

// ❌ 错误：含 | 的裸值会被解析器识别为运算符
terminal_command='grep "error" app.log | head -20'
```

---

## 快速检查清单

- [ ] Windows 下是否用了 Git Bash（不是 PowerShell）？
- [ ] 路径是否使用正斜杠 `/`？
- [ ] 含空格的路径是否用 `\"` 包裹？
- [ ] 含 `|` 的命令是否用 assign 变量？
