---
description: llm-agent Java 开发规范，包含工具类优先级、Entity、查询与分页等约定
alwaysApply: true
globs:
  - **/*.java
---

# llm-agent Java 代码规范

> **适用范围**：以下规则目前大部分是针对 **llm-agent 项目**（`llm-agent/agent-platform`、`llm-agent-framework/agent-runtime` 等模块）的代码规范。其他项目可参考但需结合实际情况调整。

## 一、工具类优先级规范

**能用工具包就优先用工具包，优先顺序：**

1. **llm-agent-framework 项目自己的工具类**（最高优先级）
   - `com.llm.agent.common.util.DateUtil`
   - `com.llm.agent.common.util.MathUtil`
   - 其他自定义工具类

2. **Apache Commons**
   - `StringUtils` - 字符串
   - `CollectionUtils` - 集合
   - `MapUtils` - Map
   - `ObjectUtils` - 对象
   - `NumberUtils`、`ArrayUtils` 等

3. **Spring Framework**
   - `StringUtils`（Spring 的）
   - `CollectionUtils`（Spring 的）
   - `ObjectUtils`（Spring 的）

4. **第三方库**（如 Guava、 Hutool 等）

**示例**：
```java
// ✅ 正确：优先使用 Apache Commons
import org.apache.commons.lang3.StringUtils;
import org.apache.commons.collections4.CollectionUtils;

// ✅ 正确：Map 使用 MapUtils
import org.apache.commons.collections4.MapUtils;

// ✅ 正确：对象判空使用 ObjectUtils
if (ObjectUtils.isEmpty(obj)) { ... }

// ❌ 错误：自行判断
if (str != null && !str.isEmpty()) { ... }
if (list != null && !list.isEmpty()) { ... }
```

## 一.1 llm-agent-framework / llm-agent-core 源码查找规则

**查找顺序**：

1. **优先使用本地源码工程**：
   - `F:\gientech-repository\llm-agent-framework`
   - `F:\gientech-repository\llm-agent-core`
2. **备选方案**：当本地源码工程不存在、未正确加载，或当前环境无法直接查看源码时，使用 jar 包中的源码

**jar 源码位置**：`F:\gientech-repository\llm-agent\lib`

| jar 包 | 说明 |
|-------|------|
| `agent-auth-1.0.0-sources.jar` | 认证模块源码 |
| `agent-common-1.0.0-sources.jar` | 公共工具类源码 |
| `agent-core-1.0.0-sources.jar` | llm-agent-core 模块源码 |
| `agent-extension-1.0.0-sources.jar` | 扩展模块源码 |
| `agent-runtime-1.0.0-sources.jar` | 运行时源码 |

**使用方式**：

1. **在 IDEA 中附加源码**：
   - 右键项目 → `Open Module Settings` → `Dependencies`
   - 选择对应的 jar → 点击 `Attach Javadoc` 或 `Attach Sources`
   - 选择对应的 `*-sources.jar` 文件

2. **快捷查看**：按住 `Ctrl` 点击类名或方法名，IDEA 会自动打开源码

**模块对应关系**：

| 本地源码工程/模块 | jar 包 |
|------------------|--------|
| `llm-agent-framework/agent-auth` | `agent-auth-1.0.0-sources.jar` |
| `llm-agent-framework/agent-common` | `agent-common-1.0.0-sources.jar` |
| `llm-agent-framework/agent-common-lite` | `agent-common-1.0.0-sources.jar` |
| `llm-agent-framework/agent-extension` | `agent-extension-1.0.0-sources.jar` |
| `llm-agent-framework/agent-runtime` | `agent-runtime-1.0.0-sources.jar` |
| `llm-agent-core` | `agent-core-1.0.0-sources.jar` |

**建议说明**：

- 规范、排查问题、阅读底层实现时，优先看本地工程源码，便于全局搜索与跟踪调用链
- 如果本地 `llm-agent-framework` 或 `llm-agent-core` 工程不存在，再退回到 `llm-agent/lib` 下对应的 `*-sources.jar` 查看源码

---

## 三、时间工具类

**优先使用项目中的 `DateUtil` 工具类，禁止使用第三方日期工具。**

### DateUtil 常用方法

```java
import com.llm.agent.common.util.DateUtil;

// 日期转字符串
String dateStr = DateUtil.formatLocalDate(localDate, DateUtil.SHORT_DATE, Locale.ENGLISH);

// 字符串转日期
LocalDate date = DateUtil.strToLocalDate("2026-05-29");

// 日期时间转字符串
String dateTimeStr = DateUtil.formatLocalDateTime(localDateTime);

// 生成日期标签序列（用于图表）
List<String> fullDateLabels = DateUtil.generateFullDateLabels(startTime, endTime);
```

### 注意事项

- 不使用 `java.time` 以外的日期类型（如 `java.util.Date`）
- 不使用第三方日期库（如 Joda-Time）

---

## 四、Entity 实体类规范

### 2.1 必须继承 BaseEntity

```java
@Data
@EqualsAndHashCode(callSuper = true)
@Entity
@Table(name = "表名")
public class MyEntity extends BaseEntity {
    // 属性定义
}
```

### 2.2 禁止使用多对多、一对多映射

**禁止**：禁止在实体类中使用以下 JPA 注解
- `@ManyToMany`
- `@OneToMany` / `@ManyToOne`（如无必要，不使用）
- `@JoinTable`
- `@JoinColumn`

**原因**：
- 增加复杂度，难以维护
- 容易引发懒加载异常（N+1 查询问题）
- GenericService 的 `searchByModel` 方法不支持关联查询

**正确做法**：
- 使用单表操作，通过 `relation_id` 或 `biz_id` 字段关联
- 复杂查询使用 `QLInfo.buildQl` 或 `QLInfo.buildSqlInfo`
- 关联数据在业务层处理

### 2.3 示例对比

```java
// ✅ 正确：使用 relationId 关联，不使用 JPA 映射
@Entity
@Table(name = "llm_tool_integration")
public class LlmToolIntegration extends BaseEntity {

    /** 关联原始实体的ID */
    @Column(name = "relation_id", length = 64)
    private String relationId;

    /** 关联的业务ID */
    @Column(name = "biz_id")
    private Long bizId;
}

// ❌ 错误：使用一对多/多对多映射
@Entity
public class Order extends BaseEntity {
    @OneToMany(mappedBy = "order")
    private List<OrderItem> items;  // 禁止！
}
```

---

## 五、注释规范

### 5.1 类注释 — 使用 Javadoc 多行注释

新建类必须添加类注释，`@author` 固定为 `jun.liu61`，`@date` 为当前创建时间：

```java
/**
 * 专家知识转换服务
 * 负责将点赞数据自动或手动转换为专家知识
 *
 * @author jun.liu61
 * @date 2025-10-15 10:11:34
 */
@Service
public class ExpertKnowledgeConvertService {
    // ...
}
```

### 5.2 POJO 属性注释 — 使用单行注释 `/** */`

```java
@Data
public class UserDTO {

    /** 用户ID */
    private Long id;

    /** 用户名称 */
    private String name;

    /** 用户状态：0-禁用，1-启用 */
    private Integer status;

    /** 是否删除 */
    private Boolean deleted;
}
```

### 5.3 静态常量注释 — 使用单行注释 `/** */`

```java
public class Constants {

    /** 缓存 key 前缀 */
    public static final String CACHE_KEY_PREFIX = "cache:";

    /** 默认分页大小 */
    public static final int DEFAULT_PAGE_SIZE = 10;
}
```

### 5.4 方法注释 — 使用 Javadoc 多行注释

**格式说明**：
- `@param` 参数说明：参数名 + 描述，可为null时说明
- `@return` 仅在有返回值时添加，无返回值不加
- `@see` 仅在有关联方法需要参考时添加
- `@throws` 仅在有异常抛出时添加
- `@eg.` 示例：逻辑复杂多变时可添加举例说明

```java
/**
 * 将指定源文件移动到目标目录
 *
 * @param srcFile 要移动的源文件，不能为null
 * @param destDir 目标目录，源文件将移动到此目录中，不能为null
 * @param createDestDir 是否自动创建目标目录（如果不存在）
 *                      true - 如果目标目录不存在，则自动创建
 *                      false - 如果目标目录不存在，则抛出异常
 * @return 移动后的目标文件路径
 * @eg.
 * // 创建不存在的目标目录并移动文件
 * FileUtils.moveToDirectory(file1, new File("target/dir"), true);
 *
 * // 目标目录必须存在才能移动
 * FileUtils.moveToDirectory(file2, new File("existing/dir"), false);
 */
public File moveToDirectory(File srcFile, File destDir, boolean createDestDir) {
    // ...
}
```

### 5.5 Record 定义注释 — 使用行尾单行注释 `/** */`

**注意**：IDEA 对 record 的 `/** */` 注释会有警告，推荐使用行尾注释 `/* */` 格式。

```java
// ✅ 正确：行尾注释格式
public record LlmPromptQueryReq(
        String name,          /* 提示词名称 */
        String agentName,     /* 所属Agent名称（对应LLMAgentStore.agentName） */
        String modelType      /* 模型类型（可空） */
) {
}

// ✅ 也可以：单行 /** */ 但IDEA会有警告
public record LlmPromptQueryReq(
        /** 提示词名称 */
        String name,
        /** 所属Agent名称 */
        String agentName
) {
}

// ❌ 错误：Javadoc 风格（record 不需要类级别注释）
/**
 * 查询请求DTO
 *
 * @author jun.liu61
 * @date 2026-05-29
 */
public record QueryReq(String name) {  // 不要这样写
}
```

**无返回值的示例**：
```java
/**
 * 保存用户信息
 *
 * @param user 用户信息，不能为null
 */
public void saveUser(User user) {
    // ...
}
```

### 3.4 类注释 — 使用 Javadoc 多行注释

```java
/**
 * 用户数据传输对象
 * 用于用户模块的接口数据交换
 *
 * @author jun.liu61
 * @date 2026-05-29 09:00:00
 */
@Data
public class UserDTO {
    // ...
}
```

---

## 七、MySQL 查询规范

### 4.1 新模块优先使用 GenericService + QLInfo

**推荐写法**：使用 `GenericService` + `QLInfo` + `andClause`

```java
@Autowired
private GenericService genericService;

// 分页查询
public PageInfo<MyEntity> pageQuery(MyQueryReq req) {
    QLInfo<MyEntity> qlInfo = QLInfo.buildQl(
        " from " + MyEntity.class.getName() + " a ",
        MyEntity.class
    );

    // 动态条件：第一个参数为 true 时添加条件
    qlInfo.andClause(req.getId() != null, " a.id = ?", req.getId());
    qlInfo.andClause(StringUtils.isNotBlank(req.getName()), " a.name like ?", "%" + req.getName() + "%");
    qlInfo.andClause(req.getStartTime() != null, " a.createTime >= ?", req.getStartTime());
    qlInfo.andClause(req.getEndTime() != null, " a.createTime <= ?", req.getEndTime());

    // 分页排序
    qlInfo.setSort("a.createTime");
    qlInfo.setDir("DESC");
    qlInfo.setPageNoAndPageSize(req.getCurrent(), req.getSize());

    // 查询
    PageInfo<MyEntity> pageResult = genericService.searchByQl(qlInfo, true);
    return pageResult.convertData(entity -> {
        MyDTO dto = new MyDTO();
        ModelUtil.copy(entity, dto);
        return dto;
    });
}
```

### 4.2 条件查询 `andClause` 用法

```java
// 基础用法：条件为 true 时添加
qlInfo.andClause(condition, " a.field = ?", value);

// 字符串模糊查询
qlInfo.andClause(StringUtils.isNotBlank(name), " a.name like ?", "%" + name + "%");

// 日期范围查询
qlInfo.andClause(startTime != null, " a.createTime >= ?", startTime);
qlInfo.andClause(endTime != null, " a.createTime <= ?", endTime);

// IN 查询
qlInfo.andClause(!CollectionUtils.isEmpty(statusList),
    " a.status in (" + String.join(",", Collections.nCopies(statusList.size(), "?")) + ")",
    statusList.toArray());
```

### 4.3 权限控制 `setDisablePermissionFilter`

```java
// 禁用权限过滤，所有人查得到
qlInfo.setDisablePermissionFilter(true);

// 注意：实体需要映射用户字段（createBy）
// 系统会根据表的用户ID自动匹配数据权限
```

### 4.4 复杂多表查询 — 使用块字符串写法

**适用场景**：多表关联查询，且实体没有 JPA 映射关系

```java
// 临时处理：后续可抽取数据到宽表
String sql = """
    select t.id as workflowTaskId,
           i.task_name as taskName,
           p.comment as nodeName,
           i.request_id as requestId,
           i.id as instanceId,
           t.status as status,
           i.status as instanceStatus,
           i.result as errorMessage,
           DATE_FORMAT(t.create_time, '%Y-%m-%d %H:%i:%s') AS createTime,
           DATE_FORMAT(t.update_time, '%Y-%m-%d %H:%i:%s') AS updateTime
    from llm_workflow_task t
         join llm_agent_instance i on t.process_instance_id = i.id and i.instance_source = ?
         join llm_agent_plan p on p.id = t.node_id
    where i.status in (?,?)
    """;

QLInfo<TaskAbnormalDTO> qlInfo = QLInfo.buildSqlInfo(sql, TaskAbnormalDTO.class,
    LLMAgentInstance.INSTANCE_SOURCE_WORKFLOW,
    ITaskStatus.STATUS_TIMEOUT_EXIT,
    ITaskStatus.STATUS_FAILED);

qlInfo.setSort("t.create_time");
qlInfo.setDir("ASC");
```

### 4.5 不使用 JPA Repository 的场景

- 新模块没有定义 `@Repository` 接口
- 需要动态条件查询
- 需要自定义 SQL（多表关联、聚合等）
- 需要禁用权限过滤

---

## 八、DTO 转换规范

### 5.1 使用 ModelUtil.copy

```java
import com.llm.agent.common.db.util.ModelUtil;

public MyDTO convert(MyEntity entity) {
    MyDTO dto = new MyDTO();
    ModelUtil.copy(entity, dto);
    return dto;
}
```

### 5.2 分页结果转换

```java
return pageResult.convertData(entity -> {
    MyDTO dto = new MyDTO();
    ModelUtil.copy(entity, dto);
    return dto;
});
```

---

## 九、事务注解规范

### 6.1 关联查询需要懒加载时添加 `@Transactional(readOnly = true)`

**背景**：当实体类使用了 JPA 映射注解（如 `@ManyToOne`、`@OneToMany`），且使用懒加载（`FetchType.LAZY`），同时需要获取关联表数据时，需要在查询方法上添加 `@Transactional(readOnly = true)`，这样 Hibernate 才会自动加载关联实体的数据。

**示例场景**：
```java
@Entity
@Table(name = "llm_tool_set")
public class LLMToolSet extends BaseEntity {

    /** 关联的 MCP Server */
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "mcp_server_id")
    private LlmMcpServer mcpServer;
}
```

**需要添加的场景**：
```java
@Override
@Transactional(readOnly = true)  // 有懒加载关联且需要获取时添加
public List<LLMToolSet> listByStatus(Integer toolStatus, String mcpStatus) {
    QLInfo<LLMToolSet> qlInfo = QLInfo.buildQl(
        " from " + LLMToolSet.class.getName() + " a ",
        LLMToolSet.class
    );
    qlInfo.andClause(true, " a.toolStatus = ? " + operator + " a.mcpStatus = ?", toolStatus, mcpStatus);
    return genericService.searchByQl(qlInfo).getData();  // 返回结果会包含 mcpServer 关联数据
}
```

**不需要添加的场景**：
- 没有懒加载关联映射
- 不需要获取关联表数据，只查主表字段

```java
@Override
// 不需要 @Transactional，纯查询且无懒加载关联
public List<LLMToolSet> listByStatus(Integer toolStatus) {
    QLInfo<LLMToolSet> qlInfo = QLInfo.buildQl(
        " from " + LLMToolSet.class.getName() + " a ",
        LLMToolSet.class
    );
    qlInfo.andClause(true, " a.toolStatus = ?", toolStatus);
    return genericService.searchByQl(qlInfo).getData();  // 只查主表字段
}
```

### 6.2 手动指定关联加载（备选方案）

如果不方便加 `@Transactional(readOnly = true)`，可以使用 `setFetchProfiles` 手动指定：

```java
@Override
public List<LLMToolSet> listByStatus(Integer toolStatus) {
    QLInfo<LLMToolSet> qlInfo = QLInfo.buildQl(
        " from " + LLMToolSet.class.getName() + " a ",
        LLMToolSet.class
    );
    qlInfo.andClause(true, " a.toolStatus = ?", toolStatus);
    // 手动指定加载 mcpServer 关联
    qlInfo.setFetchProfiles("a.mcpServer");
    return genericService.searchByQl(qlInfo).getData();
}
```

### 6.3 写操作事务

INSERT/UPDATE/DELETE 使用 `@Transactional`（不带 readOnly）：

```java
@Override
@Transactional  // 写操作使用，不带 readOnly
public void save(LLMToolSet entity) {
    genericService.merge(entity);
}
```

### 6.4 注意事项

- `QLInfo.buildQl()` 拼装的 SQL，表别名**只能是 `a`**
- 纯查询方法没有懒加载关联时，**不需要添加任何事务注解**
- 写操作使用 `@Transactional`（不带 readOnly）

---

## 十、快速检查清单

### 工具类使用
- [ ] 是否优先使用了项目自己的工具类？
- [ ] String 是否使用 StringUtils？
- [ ] Map 是否使用 MapUtils？
- [ ] Collection 是否使用 CollectionUtils？
- [ ] 对象是否使用 ObjectUtils？

### 时间工具
- [ ] 是否使用了 `DateUtil` 而非 `Date` 或第三方库？

### Entity 规范
- [ ] 是否继承了 `BaseEntity`？
- [ ] 是否避免了 `@OneToMany`/`@ManyToMany` 映射？

### 注释规范
- [ ] 新建类是否有 Javadoc 类注释（@author 固定为 jun.liu61）？
- [ ] 属性是否使用 `/** */` 单行注释？
- [ ] 常量是否使用 `/** */` 单行注释？
- [ ] public 方法是否有完整的 Javadoc（@param、@return 等）？
- [ ] 无返回值方法是否省略了 @return？
- [ ] record 类型的字段注释是否使用行尾注释格式 `/* */`？

### 查询规范
- [ ] 新模块是否使用了 `GenericService` + `QLInfo`？
- [ ] 动态条件是否使用 `andClause`？
- [ ] 需要禁用权限时是否调用了 `setDisablePermissionFilter(true)`？
- [ ] 复杂 SQL 是否使用了 `"""` 块字符串？
- [ ] QLInfo 拼装的 SQL 别名是否为 `a`？

### 事务规范
- [ ] 涉及懒加载关联且需要获取关联数据时，是否添加了 `@Transactional(readOnly = true)`？
- [ ] 没有懒加载关联的纯查询方法，是否没有添加事务注解？
- [ ] 写操作是否使用 `@Transactional`（不带 readOnly）？
